{
  "name": "byte-of-me",
  "private": true,
  "version": "0.2.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm generate && pnpm --filter web build",
    "generate": "pnpm --filter @repo/db generate",
    "lint": "pnpm --filter web lint",
    "format": "pnpm --filter web format"
  }
}