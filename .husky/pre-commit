#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Fast gate on every commit: type-check + lint (turbo caches, so this is quick).
# The full build runs on pre-push instead — see .husky/pre-push.
turbo run check-types lint || exit 1
